class Solution:
    def minPartitions(self, n: str) -> int:
        return max(n)

# structure looks like stacking addition with just 1s and 0s
# there for the minimum number of partitions is = the highest number = number of stacks